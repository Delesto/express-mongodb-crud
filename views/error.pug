<!DOCTYPE html>
html(lang="ru")
    head
        include includes/head.pug
    body#error
        
        div.error-container
            if status === 404
                h1.error-status #{status}
                p.error-message #{message}
                button.error-back Назад
            else
                h1.error-status #{status}
                p.error-message #{message}
    include includes/scripts
    if status !== 500
        script(src="/js/backFrom404.js")